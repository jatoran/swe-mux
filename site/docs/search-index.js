/* Generated by site/tools/build.py from site/tools/docs_content.py.
   Do not edit: run the build. A classic script rather than JSON so it
   loads over file:// as well as over https://. */
window.__MUXDOCS = [{"u":"install/","t":"Install","s":"Install swe-mux from PyPI with uv, pipx, or pip, on Windows, Linux, or macOS. What each method leaves you with, and the two things no method does.","h":[["Before you start","before-you-start"],["Install it","install-it"],["Two things no install does","two-things-no-install-does"],["The window and the tray, on Windows","the-window-and-the-tray-on-windows"],["Start it","start-it"],["If nothing is on your PATH afterwards","if-nothing-is-on-your-path-afterwards"],["Platform support, stated exactly","platform-support-stated-exactly"],["Upgrading, and from a checkout","upgrading-and-from-a-checkout"],["On-device speech is a separate extra","on-device-speech-is-a-separate-extra"]],"b":"Install swe-mux is on PyPI. The wheel is py3-none-any - pure Python, no compiled extensions - and it already carries the built frontend, so installing needs no checkout and no Node. Before you start Python 3.12 or newer. Check with python --version . An installer. uv is the recommended one, pipx works the same way, and pip works with a caveat covered below. At least one agent CLI, already installed and logged in. swe-mux does not install, manage, authenticate, or proxy them. If you have none yet, install Claude Code, Codex CLI, or opencode first and log into it there. Node is not required. Node 22.6 or newer is needed only if you are building swe-mux from a checkout. Install it Every method below writes the same three commands: mux (the CLI), muxd (the daemon), and swe-mux (the desktop window and tray). Run exactly one. # Recommended. Isolated environment, all three commands on PATH globally. uv tool install swe-mux # The same isolated, on-PATH install, without uv. pipx install swe-mux # NOT the same act. Installs into whichever environment is currently active and # puts nothing on PATH globally, so `mux` works only inside that environment. pip install swe-mux Two things no install does No shortcut, no Start Menu entry - until you are asked: Wheels have no post-install hook and pip runs no install-time code, so this is structural rather than a step somebody forgot. What swe-mux does about it is ask once: the first time the Windows desktop shell starts, it offers to add a Start Menu entry and to start with Windows, and never asks again either way. mux install-shortcut does the same thing on demand, is idempotent, and has a --remove that takes back all three - the login entry included. No agent CLI is installed or logged in: That stays your own arrangement with each vendor. swe-mux finds the CLIs already on your machine and runs them under your own subscription. The window and the tray, on Windows There is nothing extra to install. pystray and pywebview are ordinary dependencies carrying a win32 marker, so the command above gives a Windows machine a working swe-mux and gives every other machine nothing to download. They want the WebView2 Runtime, which recent Windows builds already have. There was a [desktop] extra until 0.1.5. It still resolves, so an old script keeps working, and it now adds nothing. It went because every install got the swe-mux launcher whether or not it got the packages behind it, so skipping the extra produced a command whose only behaviour was to fail. Start it mux start returns once the daemon is answering and leaves it running: closing the terminal does not stop it. It is idempotent, so a daemon that is already serving is reported and left alone, and nothing else in the CLI starts one implicitly. swemux start # the daemon, in the background # then open http://127.0.0.1:8765 swemuxd # the same daemon, in the foreground swemuxd --shutdown # stops it, the supervisor, and every session swe-mux # Windows: the same thing in a window, with a tray icon mux doctor exits 0. It is read-only and reports on the daemon, the supervisor, the frontend build, the agent CLIs it can detect, the tailnet listener, and the background loops. It is the command that tells installed from working. If nothing is on your PATH afterwards This is the ordinary outcome of pip install , and its WARNING: The scripts ... are installed in '...' which is not on PATH scrolls past unread. # The daemon, needing no PATH setup at all: `python -m swe_mux` is exactly `muxd`. python -m swe_mux # Where the three executables went (a `Scripts` directory on Windows). python -c \"import sysconfig; print(sysconfig.get_path('scripts'))\" # Every file this install wrote, those three included. pip show -f swe-mux Platform support, stated exactly Host What is proven What is not Windows 10 or 11 The proving platform. The full gate runs there in CI, including the real ConPTY integration tests and the browser renderer suite, and it is the only host the desktop app ships on. - Linux The daemon plus a browser, on a required CI leg. A daemon starts, serves a real terminal, and exits cleanly there on every push. There is no Linux desktop app, by design. macOS The wheel installs and the CLI runs, checked on every push. The suite runs there too. That CI leg is still allowed to fail, so treat macOS as unproven and expect to debug. The honest boundary across all three: no CI job on any host starts a daemon from the published wheel . Installing and running the CLI is proven everywhere. That is not the same claim as working end to end. Upgrading, and from a checkout uv tool upgrade swe-mux # or: pipx upgrade swe-mux # Run from a checkout instead - what you want if you are changing swe-mux itself. git clone https://github.com/jatoran/swe-mux cd swe-mux uv sync npm --prefix frontend ci # only the source flow needs Node npm --prefix frontend run build # a fresh clone serves no UI until this runs once uv run swe-mux The frontend bundle is git-ignored build output, so a fresh clone serves a blank page rather than an error until that build has run once. Troubleshooting covers the symptom. On-device speech is a separate extra Local text-to-speech and local dictation are --extra voice-local , roughly 400 MB of wheels and model machinery. The Windows desktop bundle always carries it. Without it swe-mux speaks through the operating system's voice engine and dictates through the browser, which is a working configuration rather than a degraded one."},{"u":"first-session/","t":"Your first session","s":"Create a Project, open a terminal, and promote it into an agent session by typing the command you already type. What to expect the first time.","h":[["Create a Project first","create-a-project-first"],["Open a terminal, then type your CLI","open-a-terminal-then-type-your-cli"],["The Run menu, for everything you did not want to type","the-run-menu-for-everything-you-did-not-want-to-type"],["A CLI swe-mux has never heard of still works","a-cli-swe-mux-has-never-heard-of-still-works"],["Checking swe-mux can see your CLI","checking-swe-mux-can-see-your-cli"],["The two chords worth learning now","the-two-chords-worth-learning-now"]],"b":"Your first session There is no special ritual for starting an agent. You open a terminal and type the command you already type, and the terminal you are standing in becomes an agent session in place. Create a Project first Nothing works until there is one, and nothing is spawned until you ask for it. A Project is a folder swe-mux is pointed at, and it is what sessions, layout, notes, files, history, and per-Project settings all bind to. Point your first one at a repository you already work in. Projects covers the rest. Open a terminal, then type your CLI With a Project selected, press Ctrl + Shift + Enter . That opens a real terminal at the Project's root - a plain shell, nothing more. Type claude , codex , or whichever CLI you use, exactly as you normally would. The terminal is promoted in place. Same pane, same scrollback, now carrying a transcript, a status, a prompt queue, and a context meter. swe-mux puts its own launchers first on that terminal's PATH. Give it something to do, and watch the pane's status strip. Working , ready , awaiting , and blocked mean the same thing whichever vendor's CLI produced them. While it is mid-turn, open the Queue tab in the utility drawer and stage your next message. It is durable and head-of-line, and automatic delivery is off by default : a queued message waits for you to send it. the pane's status strip stops saying working , and the Transcript tab has something in it. A session sitting on awaiting is the agent waiting on you rather than a stall. The Run menu, for everything you did not want to type It starts an agent, a shell, a worktree session, or a task discovered in the repository: VS Code tasks, root package.json scripts, and .swe-mux/actions.toml . An imported task stays inert until its exact current bytes are approved, and any edit revokes that approval - so a task file that changed under you cannot run on yesterday's permission. A CLI swe-mux has never heard of still works swe-mux is a terminal multiplexer before it is anything else. Any shell, any TUI, any CLI runs in a real pseudoterminal exactly as it does outside. A harness the registry does not know works perfectly; what it does not get is the layer on top - normalized status, transcripts, cross-vendor history, account switching. Sessions and harnesses has the detail. Checking swe-mux can see your CLI swemux harnesses # every harness in the registry, with its detection state Settings, Harnesses shows the same thing plus the executable each one resolved to. If it resolved to the wrong binary, or to none, set the path for that harness there. That is the usual fix on a machine carrying several installs of the same CLI. The two chords worth learning now Chord What it does Ctrl+Alt+T A terminal at the current Project's root Ctrl+Alt+P The command palette, which reaches everything else The full default set is on the keyboard reference , and every one of them is rebindable."},{"u":"phone/","t":"Reach it from your phone","s":"The phone client is the same application over your own Tailscale tailnet: no relay, no swe-mux login. How to set it up, and what the access boundary actually is.","h":[["Setting it up","setting-it-up"],["HTTPS is not optional for the microphone or the clipboard","https-is-not-optional-for-the-microphone-or-the-clipboard"],["Understand the access boundary before you leave it running","understand-the-access-boundary-before-you-leave-it-running"],["What parity means here","what-parity-means-here"],["Development servers, without a raw port","development-servers-without-a-raw-port"]],"b":"Reach it from your phone The phone client is the same application, not a companion. It goes over your own tailnet with no relay in the path and no swe-mux account, because there is no swe-mux server for an account to live on. Setting it up Install Tailscale on the host machine and on the phone, on the same tailnet. Leave the daemon running. It listens on loopback and on the machine's detected Tailscale address; muxd --local-only is how you stop it doing the second one. On the phone, open the machine's .ts.net hostname over HTTPS. Turn Use Tailscale DNS on, and leave Android's Private DNS off or automatic. The certificate is bound to the hostname, so the raw 100.x address cannot serve HTTPS at all. Install it to the home screen when the browser offers. It is a progressive web app, so it gets its own window and can receive push notifications. the workspace renders on the phone with your Projects in it, and a terminal pane accepts input. HTTPS is not optional for the microphone or the clipboard Browsers restrict both outside a secure context. swe-mux puts Tailscale Serve on port 443 in front of the daemon for exactly this, and starts it at boot. Port 443 rather than the daemon's own port is deliberate: the daemon binds its port on the tailnet address directly for the plain-HTTP fallback, and a Serve on the same port would collide with it. tailscale serve status # confirm it proxies to the daemon's real port Understand the access boundary before you leave it running Tailscale policy is the entire access boundary. There is no separate swe-mux login, so any device your tailnet admits to that listener has terminal and code-execution authority on the host, equal to the account running the daemon. Binding 0.0.0.0 , a LAN interface, port forwarding, and Tailscale Funnel are unsupported configurations rather than discouraged ones . They put a terminal on a network that has no policy in front of it. What parity means here swe-mux has no feature that exists on the desktop and not on the phone. The only desktop-only code paths are hidden terminal pre-warming, the collapsed sidebar rail, and keyboard chords a phone cannot produce. One session can be attached from several devices at once. Exactly one connection may write, and the terminal size is arbitrated rather than fought over, so the phone does not reflow the desktop out from under you. The phone renders a single-pane projection of the same workspace tree rather than a second layout. Development servers, without a raw port A local dev server your session started is proxied through the daemon's own URL, hot module reload included, so the phone never needs to reach a raw port on the host. Notes, files, and previews covers the Preview tab."},{"u":"agent-setup/","t":"Have an agent set it up","s":"Paste one line into Claude Code, Codex, or any agent that can fetch a URL, and it reads a guide written for walking a person through installing swe-mux.","h":[["The line to paste","the-line-to-paste"],["What the guide covers","what-the-guide-covers"],["What it tells the agent about itself","what-it-tells-the-agent-about-itself"],["It names its own authorities","it-names-its-own-authorities"]],"b":"Have an agent set it up If you already run a coding agent, the fastest install is to hand the job to it. There is a guide written for that audience specifically, and it is plain Markdown rather than a page, so an agent gets the text and not a layout. The line to paste Help me understand and set up swe-mux. Read https://swemux.dev/agent-guide.md first, then walk me through it step by step. The guide is https://swemux.dev/agent-guide.md . It tells your agent to ask before it installs anything. What the guide covers What swe-mux is, and the three properties worth explaining before anything is installed. The prerequisites to check, and which failures should stop the install. Every install method, what each one leaves behind, and the two things none of them do. First run, and mux doctor as the verification step. The concepts to explain and the order to explain them in, which is deliberately not the order a feature list would use. Where configuration and data live on each host. The phone setup, and the access boundary stated plainly. Exactly what leaves the machine, in the same terms this reference uses. What it tells the agent about itself The last section of the guide is rules for the agent rather than for you, and they are the part worth holding it to: Verify, do not assume: After each stage, run the command that proves it worked and read the real output. mux doctor is that command for the install as a whole. Do not invent commands or flags: A command that does not exist costs a person whose install is already broken the one thing they came for. Do not run destructive commands: Nothing in a swe-mux install needs elevation, and nothing needs an existing directory removed. Say what was skipped: If it skipped the phone step or the desktop extra, it should say so and say what you gave up. Your agent CLIs are not its to reconfigure: swe-mux reads their state. Changing their settings, accounts, or transcripts is out of scope for a setup pass. It names its own authorities The guide states which sources win when it disagrees with them, and it carries the date it was revised and the version it was checked against. That is what makes it safe for it to go stale: a reader is told where the authority is rather than being left to find out."},{"u":"projects/","t":"Projects and Groups","s":"A Project binds a folder to sessions, layout, notes, files, history, and its own settings. What that means in practice, and which switches are per-Project.","h":[["What a Project owns","what-a-project-owns"],["Groups are optional organisation above Projects","groups-are-optional-organisation-above-projects"],["Per-Project rather than global, and why","per-project-rather-than-global-and-why"],["The Project context card","the-project-context-card"],["What a Project does not do","what-a-project-does-not-do"]],"b":"Projects and Groups A Project is a folder swe-mux is pointed at, and it is the thing everything else binds to. Nothing works until there is one. What a Project owns Sessions: Every terminal and agent session is spawned into a Project and stays in its sidebar group for life. Stepping into a worktree does not re-home a session: a worktree is the same Project as the tree it was cut from. Workspace layout: Panes, tabs, and split geometry are durable per-Project state on the desktop. The phone renders a projection of the same tree rather than a second layout. Notes and files: A Project-owned note collection and a bounded file tree with ignore rules. Both open into a pane rather than only into the drawer. History: Conversations are indexed against the Project they ran in, so a search can be scoped to one repository or run across all of them. Its own settings: Most switches that decide how much swe-mux does live here rather than globally. That is what lets the control plane be on for one repository and off for every other. Groups are optional organisation above Projects A Group is sidebar structure and nothing more. It does not change what a Project owns, and a Project does not need one. Per-Project rather than global, and why The control plane, automation observers, the scan timeline, the code graph, and the land queue are all off by default and enabled per Project . The reason is that they cost something - a model call, a scan budget, a graph build - and most of a person's Projects are not the one they want that spent on. A second rule sits on top of that one: a gate can only ever turn something on . Many surfaces may switch a thing on in place; exactly one editor may switch it off. For automation that editor is the Automation dashboard's Policy tab, which holds both the install-wide ceiling and every per-Project opt-in. The Project context card A Project can carry a short user-owned description of what it is and how it is meant to be worked in. It is a fixed file with an editor and bounds, it is yours rather than generated, and control-plane features that need context read it instead of guessing. What a Project does not do It does not copy, move, or rewrite anything in the folder it points at. It does not commit, and it does not run anything you did not ask for. Registering a Project spawns no session. Nothing starts until you start it. .swe-mux/ inside a checkout is per-machine state. Nothing in it is meant to be committed, and swe-mux writes a .gitignore that says so - unless you already wrote one, which is never overwritten. Switching Projects: Ctrl + Shift + 1 through 9 activate the first nine in sidebar order, and the command palette reaches the rest."},{"u":"sessions/","t":"Sessions, terminals, and harnesses","s":"A session is one pseudoterminal with a process in it. What swe-mux owns, what survives a restart, and what a recognised harness gets that an unrecognised one does not.","h":[["Two kinds, and one difference between them","two-kinds-and-one-difference-between-them"],["A CLI the registry has never heard of still works","a-cli-the-registry-has-never-heard-of-still-works"],["Sessions that outlive the application","sessions-that-outlive-the-application"],["What survives a crash is a separate mechanism","what-survives-a-crash-is-a-separate-mechanism"],["One session, several devices","one-session-several-devices"],["Launch profiles","launch-profiles"],["An ended session stays readable","an-ended-session-stays-readable"]],"b":"Sessions, terminals, and harnesses A session is one pseudoterminal with a process in it. Everything else swe-mux does is built on owning that pseudoterminal rather than on wrapping the program inside it. Two kinds, and one difference between them Shell session: A real terminal running your shell. PowerShell, CMD, bash, a WSL distro shell, anything. It behaves exactly as it does outside swe-mux. Agent session: The same thing, with a harness swe-mux recognises running inside it. The difference is entirely the layer on top: normalized status, a transcript, a prompt queue, a context meter, cross-vendor history, and account switching. A shell becomes an agent session in place when you type the CLI's command, because swe-mux puts its own launchers first on that terminal's PATH. Same pane, same scrollback. There is no separate start-an-agent action to learn, and the Run menu's agent entries are a convenience rather than the mechanism. A CLI the registry has never heard of still works swe-mux is a terminal multiplexer first. Anything that runs in a terminal runs here unchanged. An unrecognised CLI gets a real pseudoterminal and nothing else; a recognised one gets the layer. A harness list is not a support list. Sessions that outlive the application A supervisor process, separate from the daemon and from the UI, can hold every pseudoterminal. With it on, restarting the daemon or rebuilding the desktop app leaves the agents working, and reconnecting replays only the bytes you missed rather than the whole scrollback. It ships off . Turn it on in Settings, Terminals. With it off, in-process spawning is the fallback and a daemon restart reaps every session - which is why the restart endpoint refuses outright unless it is forced. What survives a crash is a separate mechanism Cold recovery is independent of the supervisor and ships on . A durable registry row is written when a session is registered, carrying an open marker; a clean shutdown closes it and a crash closes nothing, which is the entire signal. Sessions whose daemon and terminal owner both died come back as visible, dead, resumable rows rather than vanishing. Terminal checkpoints are the optional half: a bounded slice of scrollback per session, so a recovered row is readable rather than merely present. Setting the checkpoint size to zero keeps the registry - which is the part that brings sessions back - and stores no terminal bytes. One session, several devices A session can be attached from the desktop and the phone at once. Exactly one connection may write , and the terminal size is arbitrated rather than fought over, so a phone attaching does not reflow a desktop pane. Which device counts as the one you are at is decided once, application-wide, rather than guessed separately by each feature. Launch profiles A named shell or agent launch: which executable, which arguments, which working directory. A WSL distro shell is a profile like any other. Profiles are edited in Settings, Harnesses and listed by mux profiles . The WSL agent bridge reports whether the distro can actually reach the daemon rather than assuming it. Its failure mode is silence: a bridged agent that cannot reach the daemon runs perfectly and never reports. An ended session stays readable A session whose process exited does not disappear from the sidebar. It stays as a readable, resumable row, because the thing you most often want after an agent finishes is to read what it did."},{"u":"status/","t":"Session status","s":"Working, ready, awaiting, blocked: one status vocabulary across every vendor's CLI, how it is read, and why awaiting is the one to watch.","h":[["The vocabulary","the-vocabulary"],["Where the reading comes from","where-the-reading-comes-from"],["Approvals","approvals"],["Why a status is not a done signal","why-a-status-is-not-a-done-signal"],["When status looks wrong","when-status-looks-wrong"]],"b":"Session status Every session carries one of a small set of states, and they mean the same thing whichever vendor's CLI produced them. This is the layer the rest of the fleet view is built on: you cannot rank what needs you if you cannot tell what each session is doing. The vocabulary State What it means What to do working The agent is mid-turn and producing output. Nothing. Stage your next message in the Queue tab if you have one. ready The turn finished and the agent is waiting for input. Type, or send a queued message. awaiting The agent is blocked on you specifically: an approval, a question, a choice. This is the one to watch. It is not a stall. blocked Something outside the conversation is in the way. Read the pane. The sub-reason names what. idle Nothing is happening in the terminal. Not the same as finished. Background work can be running. idle is never reported as finished on its own. An agent waiting on you and an agent with background work still running render identically in a quiet terminal and mean the opposite, so nothing in swe-mux collapses them. Where the reading comes from Four independent sources, because no single one of them is reliable alone: Provider hooks: The structured events a CLI emits about its own turn. The strongest signal where a harness publishes one. The transcript: What the conversation file on disk says happened, read as the branching structure it actually is rather than as a flat log. The terminal itself: What is on the screen. The weakest signal, and deliberately never the one an approval decision is made from. The CLI's own state files: Whatever the harness writes about itself outside the transcript. Every transition is kept in a durable ledger, so a status that went wrong can be investigated afterwards. A watchdog exists for the case a session gets stuck reporting one state. Approvals When a harness asks permission, the decision is made from its structured permission request , never from what is on the terminal screen. There is a floor in the code that is checked before any configured mode, so no setting can reach past it. swe-mux never decides to deny. That is deliberately not a decision it makes; a denial stays yours. Approval modes are per-conversation and live in Settings, Prompt queue, Approvals. Why a status is not a done signal The prompt queue does not wait for a binary done. It waits for a readiness gate and a stability window, because a state that just changed and a state that has settled are different facts and only the second one is safe to act on. The prompt queue covers what that means when you turn automatic delivery on. When status looks wrong mux doctor carries a fleet status-health check, and mux doctor --export includes the status timeline's sink statistics. A status that is wrong for one session is a bug worth reporting with that export attached; a status that is wrong for every session usually means a harness updated underneath swe-mux and moved something it reads."},{"u":"control-plane/","t":"What the control plane is","s":"The layer that decides which session you look at next: deterministic facts, model-free detectors, attention ranking with an interrupt budget. Off by default, per Project.","h":[["Where the facts come from","where-the-facts-come-from"],["The layers, cheapest first","the-layers-cheapest-first"],["The refusal that makes the rest safe","the-refusal-that-makes-the-rest-safe"],["The inverse arrow: what agents read back","the-inverse-arrow-what-agents-read-back"],["Turning it on","turning-it-on"],["What it deliberately does not do","what-it-deliberately-does-not-do"]],"b":"What the control plane is The workbench is everything you look at. The control plane is the part that decides what you look at. It is off by default, per Project, and nothing in it can type, approve, or spawn. Where the facts come from Every other account of what an agent did is the transcript, which is the agent's own report of itself. swe-mux owns the pseudoterminal, so it can record what actually happened at the boundary where it happened: content hashes computed when a tool writes rather than by reading the file back afterwards, parsed test outcomes, git tree hashes, write-then-read lineage. Everything above reads that substrate. The layers, cheapest first Deterministic facts: Captured at the tool boundary. No model, no cost per session, and no way for it to be wrong about whether a file was written. Model-free detectors: Loops and stalls, claims declared but not verified, documentation debt, and provenance gaps. They read the facts and produce findings. No model runs, so they cost nothing per session and cannot hallucinate a finding. Attention ranking: Ranked items with a hard daily interrupt budget, four a day by default . Incidents merge rather than repeat, demotion rules are mined from what you have dismissed and expire on their own, and the count of what was suppressed is always shown rather than hidden. Model-backed observers: The only layer that spends money. Each one watches a run and reports, each is gated by a per-Project opt-in and an install-wide ceiling, and each carries its own budget. The refusal that makes the rest safe An observer can never type, approve, spawn, execute a script, or change a Project. That is structural rather than a policy setting - there is no code path from an observer to an action, so there is no configuration that would grant one. The consequence is that the control plane cannot rescue a stuck agent, and is not trying to. It tells you which one to go and look at. The inverse arrow: what agents read back A per-session MCP server lets an agent pull from the control plane rather than an orchestrator pushing commands down to it: sibling session status, prior resolutions, dead ends already hit, commit provenance, Project notes, the scan timeline. Its writes are bounded to four things, and each one ends at a human by default: staging a message into another session's queue, drafting a spawn request for someone to approve, arming a watch on a session it is waiting for, and interrupting or ending a session behind a per-Project grant. It starts nothing. Turning it on Open the Automation dashboard and its Policy tab. That is the one editor that can turn an automation off in either scope, which is what keeps every other gate additive-only. Enable the install-wide ceiling for the automations you want available at all. Enable them per Project, on the same matrix. A Project with no opt-in runs nothing regardless of the ceiling. Set budgets under the Policy tab's Limits and budgets disclosure. Settings, Automation shows status and links here rather than holding a second copy of the controls. The deterministic half - facts, detectors, provenance - has no model cost and is worth having on well before the model-backed half. Automation and alerts covers the observers, and Git covers provenance. What it deliberately does not do It never actuates. No typing, no approving, no spawning, no killing. It never kills a process on suspicion. Hung processes are surfaced, not terminated. It sends nothing to a service this project operates, because there is no such service. Model-backed observers call the endpoint you configured, with your key. It fails closed. An observer that cannot run produces no finding rather than a guess."},{"u":"workspace/","t":"Panes, tabs, and the drawer","s":"The workspace is a mixed tree of panes, tabs, and splits, with a utility drawer beside it. What each drawer tab acts on, and how the phone projects the same tree.","h":[["Panes and splits","panes-and-splits"],["The utility drawer","the-utility-drawer"],["Watch here, act there","watch-here-act-there"],["On a phone","on-a-phone"],["The command palette reaches everything","the-command-palette-reaches-everything"]],"b":"Panes, tabs, and the drawer The workspace is a mixed tree of panes, tabs, and splits, with a utility drawer along the right edge. Everything in it acts on one of three things: the focused session, the active Project, or the whole application. Panes and splits Chord Action Ctrl+Alt+H Split the focused pane right Ctrl+Alt+V Split the focused pane below Ctrl+Alt+Z Zoom the focused pane, and unzoom it Ctrl+Alt+D Detach the focused pane Ctrl+Alt+Left / Right Focus the previous or next pane Ctrl+Tab / Ctrl+Shift+Tab Focus the next or previous workspace tab Split geometry is durable per-Project state, so a layout you built survives a reload and a restart. Panes can be dragged and dropped, stacked into tabs, and dissolved back out. A pane is not only a terminal. Notes, files, previews, and static documents all open into one. The utility drawer On the desktop it is an in-flow split beside the workspace with an always-visible launcher rail, so it never covers what you are working on. On a phone it is an overlay entering from the right edge. Same tabs, same order. Tab Acts on What it is for Notes the Project The note collection and the editor itself, so you can read or add to a note without losing the terminal on a phone. Files the Project A bounded file tree with editors. Picking a file opens it in a pane. Actions the focused session Rail keys, prompt templates, discovered agent skills, and the clipboard history. Everything that puts text into the terminal. Queue the focused session Stage ordered messages against a mid-turn conversation, and decide when they go. Transcript the focused session The conversation, read from the harness's own transcript. Activity the focused session What the run did: the scan timeline, the detectors' findings, and the change map of files it actually wrote. Agent the focused session What this agent is running with: its tools, its policies, and its instruction files. Git the Project Status, comparison, diff review, the worktree map, provenance, and the landing strip. Processes the focused session What this session started, and what each one is serving. Hidden by default. Schedule the Project What this Project will start later, with its own fleet-wide scope. Alerts the application The ranked attention inbox across every Project. The one application-wide fleet view with a permanent tab. Tabs can be hidden, reordered, and drawn as icons or titles in Settings, Appearance. A surface folded into another tab keeps its own command-palette entry and its own voice phrase, so nothing becomes reachable only by remembering where it went. Watch here, act there Two pairs of surfaces look like duplicates. The Queue tab is beside the terminal because deciding whether to send a message is a judgement about the agent's live state, and that state is only legible in the terminal; the Fleet Queue is a modal because it has no send button and nothing in it needs a terminal beside it. The Processes tab answers \"what is this session running\" beside the pane; the Resources dialog covers the terminal and answers the fleet-wide version. On a phone The phone renders a single-pane projection of the same workspace tree rather than a second layout, so a session you split on the desktop is still there and still in order. Navigation is a top bar with horizontal swipe between Projects, an overlay for the sidebar, and the drawer from the right edge. A touch key rail carries the keys a phone keyboard does not have, and a two-finger swipe down enters a read and select mode that keeps the on-screen keyboard out of the way. The command palette reaches everything Ctrl + Shift + P , or F1 . Every command in the application is in it, including the ones with no default chord, and every one of them is bindable to a chord or a mobile gesture. The keyboard reference has the defaults."},{"u":"queue/","t":"The prompt queue","s":"Stage ordered messages against a session that is mid-turn. Durable, head-of-line, and with automatic delivery off by default. What turning it on actually means.","h":[["Staging a message","staging-a-message"],["Automatic delivery is off by default","automatic-delivery-is-off-by-default"],["The controls, and what each one is for","the-controls-and-what-each-one-is-for"],["Agent-to-agent messages","agent-to-agent-messages"],["The Fleet Queue","the-fleet-queue"],["Seeding a new session","seeding-a-new-session"],["Scheduled runs","scheduled-runs"]],"b":"The prompt queue The prompt queue is how you get work to an agent that is busy without typing into a terminal that is not listening. It is durable, head-of-line, and bound to the conversation rather than to the pane, so it survives a pane closing and a session being resumed. Staging a message Open the Queue tab in the utility drawer while a session is mid-turn and write your next message. It sits there. Nothing is sent until either you send it or an automatic-delivery rule you turned on decides to. Head-of-line means the queue delivers in order and one at a time: the second message does not overtake the first because the first is waiting on something. Automatic delivery is off by default A queued message waits for you. If you queue three messages and expect them to flow, nothing will happen. That is the design, and it is the default most likely to catch you out. When you do turn it on, it is per conversation, and it does not wait for a binary done signal, because no such signal exists that is reliable across vendors. It waits for a readiness gate and then a stability window : the session has to be ready, and it has to have stayed ready. A state that just changed and a state that has settled are different facts, and only the second is safe to act on. The controls, and what each one is for Per-conversation default and override: Automatic delivery is decided per conversation, so one long-running worker can flow while everything else waits. Quiet hours: A window where nothing is delivered automatically, for the case where you want the agent to have work waiting when you wake up rather than to have burned through it at 3am. Emergency pause: One switch that stops all automatic delivery immediately, across every conversation. Per-item expiry: A message can be given a deadline after which it is dropped rather than delivered late into a context it no longer fits. Consecutive-send cap: A limit on how many messages can be delivered in a row without evidence that the agent replied to any of them, so a wedged session cannot be fed the whole queue. All of these are in Settings, Prompt queue, under Auto-delivery . The shipped defaults are the conservative ones. Agent-to-agent messages One session can put a message into another session's queue. The floor is stated in one sentence: a non-human sender's write ends at a human. An unsolicited message from an agent is staged for you to approve, not delivered. There are exactly two ways past that floor, and both are the receiver's authorization rather than the sender's claim. The receiving session can hold a standing grant to accept agent messages, or the message can answer a request that receiving session itself made. Answering a question somebody asked is narrower than the floor rather than an erosion of it, and the narrowing stays exactly as wide as the request: the requester alone, a fixed template, the run that asked, a cap, and it ends with the authority that accepted the request. Arming is still not delivery. A message that is allowed to be armed still goes through the same readiness gate and stability window as one you armed yourself. The Fleet Queue The application-wide view of who wrote what to whom: every staged message, its sender, and its state. It is a modal rather than a drawer tab, because it has no send button - it is where you watch, and the Queue tab is where you act. Seeding a new session A message can be staged against a session that does not exist yet. Spawning binds it on the first run, which is how a scheduled run or a spawn request arrives with its instructions already attached rather than needing a second act to deliver them. Scheduled runs Cron, interval, or one-off. A schedule is a deferred press of a button you could have pressed yourself , so it goes through the ordinary spawn path, the ordinary resume path, and the ordinary prompt queue, and grows no second authority anywhere. Definitions stay on the machine rather than in the repository, because a schedule committed to a repository would arm itself in every clone and every worktree. The Schedule drawer tab is where they live."},{"u":"git/","t":"Git, worktrees, and landing","s":"Status and diff review beside the terminal, a worktree map, commit-level provenance split into committer and contributor, and a land queue that fast-forwards only.","h":[["What it reads, and how often","what-it-reads-and-how-often"],["The worktree map","the-worktree-map"],["Commit provenance","commit-provenance"],["The land queue","the-land-queue"],["Two ways the gate is skipped, both recorded","two-ways-the-gate-is-skipped-both-recorded"],["What a running gate is allowed to tell you","what-a-running-gate-is-allowed-to-tell-you"],["Turning it on","turning-it-on"],["First-time repository initialization","first-time-repository-initialization"]],"b":"Git, worktrees, and landing The Git drawer tab reads the repository behind the Project: what changed, who changed it, which worktrees exist, and - when you turn it on - a queue that lands a finished branch behind a verification gate. What it reads, and how often Attached sessions are polled for branch, HEAD, dirty count, upstream divergence, worktree identity, and lines changed. Every session is swept once a minute regardless of whether a pane is open, because a cached observation that only refreshes while you are looking at it goes stale exactly when you stop looking. Line counts cover tracked changes only. An untracked file raises the dirty count and contributes no lines, because there is nothing to compare it against. A count that could not be measured is reported as unmeasured rather than as zero - a display that conflates the two reports a clean tree for a repository it merely failed to read. An agent pane reports its working directory through the harness's hooks rather than through the shell's prompt, because a CLI holding the terminal draws no prompt. That is what makes an agent working inside a worktree report that worktree's branch and diff rather than the primary checkout's. The worktree map One row per worktree, with its files, its changes, and the live sessions standing in it. Creating a worktree is here; so is removing one. Removal is declined rather than forced wherever Git itself would refuse: a locked worktree, one carrying submodules, one that is unclean. A removal renames the checkout out of the way and purges it in the background, so a large tree does not hold the request open, and the registration is dropped with a targeted removal rather than a global prune - a prune would take every other checkout whose directory merely happens to be missing. Commit provenance Which session and which conversation produced a commit, with a confidence, split into two roles that answer different questions: Committer: The session that ran the commit. After a batch landing this is often the one that did the integrating rather than the work. Contributor: The session whose evidence shows it authored the content. This is the one you want when you are asking who wrote a line. It is drawn from deterministic capture at the tool boundary rather than from the agent's account of its own work, and it changes nothing in the repository - no trailers, no notes, no rewritten history. The land queue Landing a finished worktree branch is three steps, and the queue runs them one branch at a time so two agents cannot land into the same trunk at once. Reconcile. Merge the current trunk into the branch, inside the worktree that owns it. Verify. Run the verification command whose exact bytes you approved . Editing the command and approving it are two separate acts through two separate routes, which is what stops an agent approving the command its own land will run. Fast-forward. Move the trunk, and only forward. Git refuses a fast-forward on divergence and refuses one that would overwrite local changes, which is what makes this step safe for a machine to take: the pipeline cannot lose work by construction. It never resolves a conflict and it never runs a gate nobody approved. A conflict or a failed gate both need judgement and both belong to the branch's own agent, so they come back to that session as a bounded message rather than being worked around here. Two ways the gate is skipped, both recorded A documentation-only change: After reconciling, the incoming paths are matched against a closed allowlist. Matching a fixed list is a total function with no model and no heuristic in it, so it is not a judgement call. Anything it cannot answer with certainty - a source file, a rename, a submodule, an unreadable diff - runs the full gate. A verdict already earned: A verify-only request runs every earlier step identically, so its verdict is the verdict a land would have produced. That verdict is kept against the git tree it ran over and the digest of the command that ran, so a later land over identical content reuses it. A moved trunk yields a new tree and the gate runs again. Both are written into the request's event trail with the reason, and the skipped step is still present in the trail. A documentation-only land would otherwise read exactly like one that passed three minutes of tests. What a running gate is allowed to tell you Every signal is observed or absent, never estimated. A step number counts markers the gate itself printed. A step total exists only where a byte-identical run has already passed, and is withdrawn the moment a run overruns it. No percentage is derived at either end, because this repository's own gate has steps that take 45 seconds and 3 seconds - a proportion drawn over that would be fiction, and a wrong number gets acted on where an absent one does not. Turning it on The land queue is off by default, per Project. It is enabled in Settings, Projects, and its queue, its verification command, and its grants live in a compact strip at the head of the Git tab's Map. A blocked row opens that strip rather than drawing a second copy of the controls. The manual two commands remain the fallback and the thing to reach for when the queue is not enabled: reconcile in the worktree, then fast-forward from the primary checkout. First-time repository initialization A Project pointed at a folder that is not a repository yet can be initialized from the Git tab. It is an explicit act; swe-mux does not create repositories under you."},{"u":"notes-files/","t":"Notes, files, and previews","s":"Project-owned notes in a real Markdown editor, a bounded file tree with editors, leased watches, and dev-server previews proxied so a phone never needs a raw port.","h":[["Notes","notes"],["Files","files"],["Previews","previews"],["Prompt templates","prompt-templates"],["The clipboard history","the-clipboard-history"]],"b":"Notes, files, and previews Three Project-scoped surfaces that open into panes rather than living only in the drawer: the note collection, the file tree, and whatever your sessions are serving. Notes A flat, Project-owned collection in a real Markdown editor - rendered headings, nested lists, checkboxes, an outline you can jump through. Notes are created from the Notes drawer, not from a terminal or a session, because a note belongs to the Project rather than to whichever session happened to be focused. Chord Action Ctrl+Alt+N Open the current Project's notes Ctrl+Alt+J Jump to a heading in the focused note Ctrl+F Find inside the focused note, from within the editor There is one global note, the Scratchpad , pinned at the top of the drawer in every scope. It lives outside every Project and every repository, it cannot be renamed or deleted, and it is the right place for something that is not about the repository you happen to be standing in. Deleting a note is recoverable. The file moves into a trash directory inside the Project's own ignored state rather than being unlinked, so the bytes are still on disk when you want them. The one refusal is emptying a Project's collection: the last note cannot be deleted, and the control says why rather than merely being disabled. The whole note tree is Project-local application state and is git-ignored by a manifest swe-mux writes at Project creation. An ignore file you already wrote is yours and is never rewritten. Files A bounded file tree over the Project root with editors, ignore patterns, and a reveal into the host file manager. Picking a file opens it into a pane, so reading code beside a running agent does not cost you the terminal. Text edits are revision-checked : an edit carries the revision it was made against and is refused if the file moved underneath it. That is the difference between a conflict you are told about and a change you silently lost. Watches are leased. A watch on a directory is non-recursive and expires unless it is renewed, which is what stops a browser tab left open for a week holding filesystem watches on a repository nobody is looking at. Previews swe-mux notices what your sessions started and what each one is listening on. A local development server can be opened as a Preview tab, and it is proxied through the daemon's own URL, hot module reload included - so a phone reaches it without ever needing a raw port on the host, and without a second thing to expose on your tailnet. A static document in the checkout can be previewed the same way, under a sandbox policy, which is how you read a built HTML report without leaving the workspace. swe-mux finds hung processes; it does not kill them. A suspected orphan is surfaced with what it is and what started it. Terminating it stays an explicit act you take. Prompt templates Saved reusable messages, in the Actions tab. Selecting one inserts and never submits : a template is text, not an action, and a template that sent itself would be a macro with no confirmation step. The clipboard history A ring of recent captures, in the Actions tab beside the templates, because both do the same thing - put text into the focused agent. On a phone this is often the only practical way to move a long string into a terminal."},{"u":"history/","t":"History and transcripts","s":"One search and one resume across every supported harness, read from the vendors' own transcript files, which are never moved, rewritten, or deleted.","h":[["What it gives you","what-it-gives-you"],["Transcripts are read, never owned","transcripts-are-read-never-owned"],["A transcript is a graph, not a log","a-transcript-is-a-graph-not-a-log"],["The Transcript tab","the-transcript-tab"],["When two entries share a conversation","when-two-entries-share-a-conversation"],["Usage is reconstructed from these","usage-is-reconstructed-from-these"]],"b":"History and transcripts History is one search and one resume across every supported harness. It reads the native transcript directories each vendor's CLI already writes, and never moves, rewrites, or deletes them . What it gives you One search over every conversation, in every Project, from every harness, rather than one search per vendor. One resume. Picking a past conversation restarts it in the harness it belongs to, through the same path a scheduled resume uses. Cross-vendor review. Reading what a Codex run did and what a Claude run did without learning two transcript formats. Project scope. A search runs across everything by default, or inside one Project. Transcripts are read, never owned Native transcript directories are reconciled at startup: swe-mux indexes what is there, and the files stay exactly where the vendor's CLI put them. Uninstalling swe-mux leaves every conversation intact, because none of them were ever its to hold. A transcript is a graph, not a log A Claude transcript is an append-only branching structure rather than a flat list. A retry, a rewind, or an edited message leaves the abandoned branch in the file. swe-mux linearizes it, which means two things worth knowing: The indexing projection drops the branches the conversation left , so a search does not return three variants of the same exchange. The human reader marks them rather than hiding them, so you can see that a retry happened. Neither of them reconstructs the live branch by walking the parent chain, because a parallel tool batch parents each result to its own call - a chain walk would drop every result but the last, and would do it silently. The Transcript tab The drawer's Transcript tab is the live version of the same reading, pinned to the focused session. It is what you read when you want to know what the agent actually said rather than what is currently on screen, and it survives the terminal being cleared. When two entries share a conversation A conversation that was resumed under two different session identities can end up as two history rows. That is reported and repairable rather than left to accumulate: swemux history-duplicates # report what would change swemux history-duplicates repair # merge each conversation's rows Usage is reconstructed from these Agent spend is not metered by swe-mux, because a subscription CLI does not report a price per call. It is reconstructed from the transcripts, which is why the usage view labels it as an estimate. Accounts, usage, and budgets explains the three separate pots and why they are never summed."},{"u":"voice/","t":"Voice and the assistant","s":"Read aloud in three policy layers, hands-free conversation with local transcription and wake words, and an assistant whose confirmation floor is not configurable.","h":[["Read aloud, in three ordered layers","read-aloud-in-three-ordered-layers"],["Which voice engine speaks","which-voice-engine-speaks"],["Talk: hands-free conversation","talk-hands-free-conversation"],["The Mux assistant","the-mux-assistant"],["Where the routing goes","where-the-routing-goes"]],"b":"Voice and the assistant Two independent halves. Read aloud speaks what an agent said. Talk listens, and turns speech into navigation, commands, dictation, or an assistant turn. Neither one needs the other, and both are off until you turn them on. Read aloud, in three ordered layers The policy is three explicit layers rather than one switch: The master switch. Off means nothing is generated or played, anywhere, by any path. Settings, Voice owns it. Per session. Does this session generate speech at all. Per device, plus one global rule. The focused session plays here; every other session holds its clip and shows it as held on that pane's strip rather than talking over you. A reply can be spoken as a summary , which costs a call to the cheap model you configured, or verbatim , which costs nothing and calls no model at all. Which voice engine speaks Engine Where it runs What it costs The operating system's voice On your machine, through the OS speech engine. Nothing. The default, and the fallback. A local neural model On your machine, from the voice-local extra. The Windows desktop bundle always carries it. Nothing per call. A one-time model download. External Edge TTS A Microsoft endpoint, and text leaves the machine. Explicitly experimental. Requires a versioned acknowledgement before anything is sent, and is never bundled. A word the local model can only spell out letter by letter is fixable with a respelling in Settings, Voice, applied without a restart. Each spell-out is recorded, so the list of words that need one is a list rather than something you have to notice. Talk: hands-free conversation Browser microphone capture, on-device voice activity detection, and local transcription. Nothing is written to disk, and the audio is decoded from memory. Wake words and commands: Both are yours to configure. The set of actions a phrase can map to is fixed - send, append, cancel, undo, mute, read, interrupt, help, standby, resume, hold, proceed, stop and a few more - and the phrases that reach them are not. Spoken navigation: Projects and sessions are addressed by number, following the order the sidebar draws them, so \"Project 2, Session 3\" resolves both coordinates at once without needing a list read out first. Standby and stop: Standby keeps the microphone on and ignores everything except a resume or a stop. Stop releases the microphone. Hold and proceed: The brainstorm pair. Plain speech buffers instead of becoming turns until \"go ahead\" releases it as one consolidated turn. Push to talk: Hold Alt + Shift + Space for capture with no endpoint detection at all. Playback leaves capture open, so you can interrupt. Speech immediately ducks the audio, waits for the echo to drain, and stops the stream if what it heard was really you - and restores playback if it was the echo. The microphone needs a secure context. On a phone that means HTTPS, which is what the Tailscale Serve step on the phone page is for. The Mux assistant Ask for something in words and have it happen. The division of labour is the whole design: the model proposes names, deterministic code resolves and executes them through the paths a button would have used. The model does not get a shell, and it does not get an API of its own. The confirmation floor for a consequential action is not configurable. There is no setting that turns it off, because a setting that turns off confirmation is the setting somebody turns off once and then forgets about. A turn has a round budget it is told about and asked to work within, and running out of rounds is announced rather than silent. Anything you say while a turn is running is queued and merged into the next fragment rather than refused, because a refusal had nowhere to go and simply lost what you said. Where the routing goes Three tiers, in order, and the assistant is the last resort rather than the first: a deterministic grammar for help, fleet queries, navigation, and reply reading; then a conservative fuzzy pass; then - only on no match, and only when it is enabled - the assistant."},{"u":"automation/","t":"Automation and alerts","s":"Model-backed observers that watch a run and report, an attention inbox with a hard daily interrupt budget, and push notifications to the device you are actually at.","h":[["What an observer can and cannot do","what-an-observer-can-and-cannot-do"],["Two outputs, and where each one lives","two-outputs-and-where-each-one-lives"],["The interrupt budget","the-interrupt-budget"],["The Automation dashboard","the-automation-dashboard"],["Budgets","budgets"],["Alerts and push","alerts-and-push"],["The deterministic detectors need none of this","the-deterministic-detectors-need-none-of-this"]],"b":"Automation and alerts Automation is the model-backed half of the control plane: observers that watch a run and report on it. It produces exactly two things, an attention item or a run note , and it can never do anything else. What an observer can and cannot do It can read a run and write a finding. It can never type, approve, spawn, execute a script, or change a Project . That is structural: there is no code path from an observer to an action, so there is no setting that would grant one and no misconfiguration that could produce one. Two outputs, and where each one lives A run note: Something worth recording that is not worth interrupting you for. It has exactly one home: the Activity tab's Findings. An attention item: Something that may need you. It appears in the Alerts drawer tab and in the Automation dashboard's Activity tab - the same component over the same data rather than a second copy of it. The interrupt budget Items are ranked , and there is a hard daily interrupt budget, four a day by default , across four channels split by how expensive the item is to resolve. Incidents merge rather than repeat. The same problem recurring is one item with a count, not fifteen. Demotion rules are mined and expire. What you dismiss teaches it what not to raise, and those rules time out rather than accumulating into a filter nobody remembers writing. The suppressed count is always shown. You are told how many items did not make the budget, and why each one did not. The Automation dashboard Tab What it holds Policy The one editor that can turn an automation off in either scope: the install-wide ceiling and every per-Project opt-in, as one matrix. Limits and budgets live behind a disclosure here. Usage What automation has spent, by automation and by Project. The same component the Usage view draws, over the same numbers. Activity The attention inbox and the run notes, at fleet scope. Settings, Automation shows status and links here rather than holding a second set of switches. Two controls writing one key is how a setting ends up with two answers. Budgets Every automation carries a cap, and a cap is tokens, dollars, or both. The reason it is both is a real limitation rather than a preference: a dollar cap cannot bind against a provider that reports no cost. Absent cost is recorded as unmeasured rather than as zero, every total drawn over it reads as a floor, and the token axis is the honest backstop. Rate limits and per-call ceilings are deliberately not budgets. They count acts and bound one request; a budget bounds a period's spend. Conflating them makes one of the two silently unenforced. Alerts and push Web push to a phone, with per-device preferences, plus sounds on the desktop. Which device you are actually at is decided once, for the whole application , from a presence heartbeat, rather than each feature guessing separately and disagreeing. Push goes through your browser vendor's push service, which is how web push works everywhere, and only after you subscribe a device. Nothing is sent to a service this project operates, because there is not one. The deterministic detectors need none of this Loops and stalls, declared-but-not-verified claims, documentation debt, and provenance gaps all run with no model at all . They cost nothing per session and cannot hallucinate a finding, and they are worth having on well before any observer is. The control plane covers the layering."},{"u":"usage/","t":"Accounts, usage, and budgets","s":"Switching between provider accounts you own, the three spend pots that are never summed, where a model call goes, and what a cap can and cannot enforce.","h":[["Provider accounts","provider-accounts"],["The three pots, and why they are never summed","the-three-pots-and-why-they-are-never-summed"],["Where a model call goes","where-a-model-call-goes"],["Budgets","budgets"],["What reaches the network, and what does not","what-reaches-the-network-and-what-does-not"]],"b":"Accounts, usage, and budgets Three separate things live near each other here and are constantly confused: the accounts your agent CLIs log in with, what has been spent, and the caps that bound future spend. Provider accounts Save, relabel, reauthenticate, switch, and remove Claude and Codex logins, with subscription-window polling so you can see how much of a window is left. Only authentication is copied, and switching is always an explicit act you take. This is for one person switching between accounts they personally own and pay for , replacing the logout and login cycle the provider CLIs otherwise require. It is not account pooling and not a way around a usage limit: accounts are never shared between people, credentials stay local and go nowhere but the provider's own endpoints, and sessions are never load-balanced across accounts. swemux accounts # what is saved, and which is active swemux accounts verify # re-verify each saved identity swemux accounts audit # the credential audit trail The three pots, and why they are never summed Pot What it is How exact it is Agent spend What your agent CLIs used, on your subscription. An estimate. It is reconstructed from transcripts, because a subscription CLI reports no price per call. Automation spend What swe-mux's own model-backed features used, on your metered key. Metered, billed by the call, and exact where the provider reports a cost. Provider quota How much of a provider's rolling window is left. Not money at all. A share of a window. No surface computes a total across them , and every figure carries the basis it was drawn on. A number without its basis is the bug, not a missing feature: adding an estimate to a metered charge and calling it a total produces a figure that is wrong in a way nobody can see. Agent spend itself has two denominators - one over every transcript on the machine, one over only the runs swe-mux observed - so the subset is labelled by its denominator everywhere it appears and is never presented as the agent total. Where a model call goes Every model setting is edited in one place : Settings, Accounts, Models. Each feature tab keeps a read-only row naming the model it resolved to, with a link back to that editor. The reason they are together rather than beside the features they configure is that changing endpoint is the operation that touches all of them at once . Split across four tabs, that operation meant four tabs of hunting to find out whether anything had broken, with no screen anywhere answering \"what does this install call, and does the endpoint I just switched to even have it\". A blank value is not always legal, and the two cases must never look the same. An override left blank falls through to the cheap model. A pin left blank is refused by the control. Budgets A cap is {tokens, dollars, mode} , edited through one control, per feature. Both axes exist because a dollar cap cannot bind against a provider that reports no cost - absent cost is unknown rather than zero, so the ledger records it as unmeasured, totals drawn over it read as a floor, and the token axis is the honest backstop. What reaches the network, and what does not Your agent CLIs contact their own vendors under your own subscription. swe-mux proxies nothing and resells nothing, and it is not in the execution path between the CLI and its vendor. Where data lives lists every request swe-mux itself can make."},{"u":"plugins/","t":"Plugins","s":"Install versioned external-process plugins, understand their authority, and publish a standalone repository to the validated swe-mux catalog.","h":[["Install from the catalog","install-from-the-catalog"],["Develop locally","develop-locally"],["Review updates","review-updates"],["Authority","authority"],["What a plugin can contribute","what-a-plugin-can-contribute"],["Author and publish","author-and-publish"]],"b":"Plugins A swe-mux plugin is an ordinary repository with swe-mux-plugin.toml at its root. It contributes commands and terminal tools without changing the swe-mux checkout or application bundle. Install from the catalog Open the plugin catalog , inspect the declared source, permissions, platforms, runtime requirements, and exact revision, then install the repository. Catalog selection never approves or enables code. swemux plugin install OWNER/REPOSITORY --ref RELEASE_TAG swemux plugin list swemux plugin approve PUBLISHER.PLUGIN The catalog supplies a literal release tag, which pins the inspected release. A branch is accepted but is a moving channel, and every changed byte still requires approval. Develop locally The configurable development root defaults to ~/swe-mux-plugins . Settings and swemux plugin discover inspect only direct child repositories with a root manifest. Discovery is inert: each candidate still needs an explicit Link action and content approval. An arbitrary repository remains linkable when a monorepo, worktree, or alternate drive should stay where it is. swemux plugin development-root ~/swe-mux-plugins --create swemux plugin discover swemux plugin link ~/swe-mux-plugins/my-plugin swemux plugin approve PUBLISHER.PLUGIN After an edit, run swemux plugin refresh , validate, and approve the current bytes. Actions pick them up on their next run. A live pane remains its old process until it is closed and relaunched or replaced with swemux plugin restart-panes PUBLISHER.PLUGIN . Review updates Managed updates are never automatic. Checking reads source-channel metadata only. Downloading creates a separate durable review stage while the active version keeps running. Settings shows exact revision and permission or capability changes before a separate approval promotes those bytes. swemux plugin check-updates swemux plugin update PUBLISHER.PLUGIN swemux plugin approve-update PUBLISHER.PLUGIN Authority A plugin process runs as your operating-system user. The permission list limits which cooperative swe-mux callback operations receive a token. It is not a sandbox for files, processes, credentials, or network access. Acquisition, catalog browsing, validation, and inspection execute no plugin code. Approval is bound to the complete source content and security-relevant manifest fields. An update review leaves the active version enabled until separate approval. Uninstall retains plugin config and state unless purge is separately confirmed. What a plugin can contribute Actions: Bounded explicit commands that exit and retain capped output in the command log. Panes: Interactive terminal tools opened as a Project tab, right-hand split, or popup. Events: Bounded commands subscribed to exact normalized application events. Startup: One bounded restoration command after enablement or daemon startup. Links: Validated terminal URL patterns routed to an action from the same plugin. Author and publish Create one standalone repository with a root manifest, directly runnable source, tests, README, and licence. Declare every platform, runtime, capability, context, and callback permission the code requires. Run swemux plugin validate . , link it locally, and exercise every contribution and lifecycle transition. Publish a GitHub release whose tag matches the manifest version, then add the swe-mux-plugin topic. swemux plugin validate . swemux plugin link . swemux plugin approve PUBLISHER.PLUGIN swemux plugin disable PUBLISHER.PLUGIN swemux plugin uninstall PUBLISHER.PLUGIN The catalog reads only public repository metadata and the manifest at an exact commit. A valid community listing is discoverable, not reviewed or endorsed."},{"u":"settings/","t":"Settings reference","s":"Every Settings tab, what it owns, and the rule that decides whether a switch is global, per Project, or per device.","h":[["Workspace","workspace"],["Agents","agents"],["Interface","interface"],["System","system"],["Which scope a setting has, and why","which-scope-a-setting-has-and-why"],["Two rules the panel is built on","two-rules-the-panel-is-built-on"],["Editing the file directly","editing-the-file-directly"]],"b":"Settings reference Settings is seventeen tabs in four groups. Ctrl + Shift + , opens it, and the sidebar is the only navigation: the longer tabs are several pages each rather than one scrolling document. Workspace Tab What it owns General Application-wide behaviour that does not belong to a subsystem below. Projects The Project registry, and most per-Project switches : the control plane opt-ins, the land queue, and the authority fields that decide what an agent may do in this repository. Terminals The PTY supervisor, session recovery and its checkpoints, multi-device input, and terminal behaviour. Git Polling cadence, comparison refs, and worktree defaults. Processes Process discovery, preview ownership, and preview lifetime. Agents Tab What it owns Harnesses Each harness, its detection state, the executable it resolved to, an override for that path, and launch profiles. Accounts Three pages: Provider accounts (saved logins and switching), Model provider (the endpoint and key), and Models - the single place every model setting in the application is edited. Prompt queue Six pages: Overview, Auto-delivery , Approvals , Agent messaging , Agent actuation , and Queue history. Automation Status only, and a link to the Automation dashboard's Policy tab, which is the one editor that can turn an automation off. Usage Spend views and the budget caps. Interface Tab What it owns Appearance Theme, density, which drawer tabs are drawn and in what order, and whether the rail shows icons or titles. Input Five pages: Pointer, Mobile terminal , Clipboard history , Touch gestures , and Keyboard shortcuts . Text editor The note and Markdown editor's own behaviour. Voice Five pages: Read aloud , Talk and dictation , Voice commands , Mux assistant , and Diagnostics. System Tab What it owns Alerts Notification preferences, per device, plus sounds. Remote The listeners, the Tailscale integration, and Serve. This is where the access boundary is configured, and the unsupported bindings are refused rather than merely warned about. Diagnostics Health, logs, the software-update check, and the frozen-app rebuild on Windows. Which scope a setting has, and why Global: Written to config.toml in the data directory. This is the daemon's own configuration, and it is what a second device sees too. Per Project: Anything that decides how much swe-mux does : the control plane, automation, the scan timeline, the code graph, the land queue, and the authority fields. The point is that most of your Projects are not the one you want a scan budget spent on. Per device: Written by the browser, in settings.json . Autoplay, gestures, notification preferences, the theme - the things that are properties of the screen you are at rather than of the install. Two rules the panel is built on Two controls never write one key. Where a value is shown in a second place, it is read-only and links to its editor. That is why every feature tab names its model without offering a picker. A gate can only turn something on. A surface that needs a switch enabled can enable it in place; turning it off is always the owning editor's. That asymmetry is what makes a write reachable from a drawer pane safe. Editing the file directly config.toml in the data directory is the same configuration, and it is schema-versioned with a pre-migration backup written beside it. Settings is the better route because it validates. A config that does not validate is one of the four common causes of a daemon that will not start, and it is the first check mux doctor reports on."},{"u":"cli/","t":"Command line reference","s":"Every mux subcommand, every muxd flag, the exit-code contract scripts branch on, and how the CLI resolves which daemon to talk to.","h":[["muxd, the daemon","muxd-the-daemon"],["mux, the client","mux-the-client"],["Which daemon mux talks to","which-daemon-mux-talks-to"],["Exit codes","exit-codes"],["swemux doctor has two modes, and picks between them itself","swemux-doctor-has-two-modes-and-picks-between-them-itself"]],"b":"Command line reference Three commands are installed: mux talks to a running daemon, muxd is the daemon, and swe-mux is the Windows desktop window and tray. muxd, the daemon Flag What it does --host Override the configured listener host. Must be a loopback address. --port Override the configured port for this run. --config Use a specific configuration file. --dev Development mode. --local-only Do not open the direct Tailscale listener for this run. Useful when you are isolating a network problem, because it removes tailnet detection from the startup path. --where Print where swe-mux is installed, whether its commands are on PATH, and how to run it when they are not. Then exit. --shutdown Reap every session the PTY supervisor owns, stop the supervisor, and exit. This ends live sessions. It is the deliberate stop-everything command, never part of an update. --host takes a loopback address only, because the Tailscale listener is detected rather than configured. Binding a LAN interface is an unsupported configuration rather than a flag that is missing. mux, the client Command What it does swemux ls List sessions. Filter with --project, --state, --backend. swemux spawn Spawn a session. --project is required; --backend, --name, --profile, --exe, and repeatable --arg shape it. swemux send Send input to a session, named by id, name, or a unique id prefix. swemux kill Terminate a session. swemux resume Resume a history entry. --project is required. swemux projects List Projects. swemux profiles List launch profiles. swemux harnesses List every harness in the registry with its detection state. The first thing to run when a CLI you have installed is not being recognised. swemux history List conversation history. swemux history-duplicates Report, or with repair merge, history entries that share one conversation. swemux accounts list , verify , or audit saved provider accounts. swemux doctor The consolidated read-only diagnostics report. --export prints the full bundle as JSON. swemux update Report the release check and this install's update path. --install VERSION downloads that release and verifies its hash against the published manifest before anything is staged. swemux reload-daemon Restart the daemon in place. Sessions survive when the PTY supervisor is on; --force restarts without it and kills every session . swemux install-shortcut Create the Start Menu and Desktop shortcuts a wheel install structurally cannot (Windows). --startup adds a run-at-login entry, --remove takes them all away. Idempotent. Every subcommand takes --json to print the raw daemon response instead of a table, and --url to point at a specific daemon. Which daemon mux talks to Resolved in this order, and the order is the contract: --url , then the MUX_URL environment variable, then the configured host and port, then http://127.0.0.1:8765 . Exit codes Code Meaning 0 Success. For doctor, a full report with no failing check. 1 A doctor report with a failing check, local or full. 2 Usage error. 3 Daemon unreachable. For doctor, also a clean local report. 4 Daemon HTTP error. 5 Ambiguous session name. 6 Not found. A degraded report never exits 0 , so a script gating on mux doctor keeps working whether or not a daemon is running. The doctor codes compose the two that already existed rather than adding a scheme of their own. swemux doctor has two modes, and picks between them itself Full report: When a daemon answers. Categories for the daemon, each harness, remote access, the firewall, host prerequisites, fleet status health, the background loops, conversation freshness, WSL, and optional assets. Nothing in it reads a secret, a terminal byte, or message content. Local report: When nothing answers, because a daemon that will not start is the most likely new-user failure and the full report presupposes exactly the thing that is missing. It checks the interpreter, the imports, the config, the frontend bundle, the data directory, the database, the port, this host's pseudoterminal backend, and the supervisor bundle. The local report marks what it did not check as unchecked rather than folding it into a pass. Folding a skipped check into ok claims health nobody measured, which is worse than the connection error it replaced."},{"u":"keyboard/","t":"Keyboard and the command palette","s":"Every default chord, the command palette that reaches the rest, and how rebinding and mobile gestures work.","h":[["The flat defaults","the-flat-defaults"],["The leader key reaches everything else","the-leader-key-reaches-everything-else"],["Presets, if you already know another tool","presets-if-you-already-know-another-tool"],["Rebinding","rebinding"],["Why not Ctrl+Alt","why-not-ctrl-alt"],["What the terminal, the browser and your desktop keep","what-the-terminal-the-browser-and-your-desktop-keep"],["Mobile gestures","mobile-gestures"],["Voice reaches the same registry","voice-reaches-the-same-registry"]],"b":"Keyboard and the command palette Three entry points cover everything: Ctrl + Shift + Enter for a terminal at the Project root, Ctrl + Shift + P for the command palette, and Ctrl + Shift + Space as a leader key that puts all 214 commands behind one chord and tells you what to press next. The flat defaults Everything below is on Ctrl + Shift , which is what every terminal emulator claims for itself, because bare Ctrl +letter belongs to the shell running inside the pane. On macOS the same set is mirrored onto Cmd + Shift ; both work. Chord Command Ctrl+Shift+Enter New terminal in the current Project Ctrl+Shift+P, or F1 Open the command palette Ctrl+Shift+Space The leader key (see below) Ctrl+Shift+\\ Split the focused pane right Ctrl+Shift+- Split the focused pane below Ctrl+Shift+Arrows Focus the pane in that direction Ctrl+Shift+Z Toggle focused pane zoom Ctrl+Shift+X Close the focused pane Ctrl+Shift+[ and ] Previous and next tab in the focused pane Ctrl+Shift+F Find in the focused terminal Ctrl+Shift+L Toggle the side panel Ctrl+Shift+E / G / U / K Side panel: files, Git, queue, transcript Ctrl+Shift+S Toggle the Projects sidebar Ctrl+Shift+H Browse session history Ctrl+Shift+, Open Settings Ctrl+Shift+1 .. 9 Activate the first nine Projects, in sidebar order Ctrl+Tab, Ctrl+Shift+Tab Next and previous tab (desktop app only) The leader key reaches everything else Press Ctrl + Shift + Space and an overlay names what the next keystroke can be, grouped one letter per area: p panes, s sessions, w Projects, v views, n notes, t terminal, g Git, d the side panel, f focus, and a few more. So a new terminal is leader, s , n ; the pane to the left is leader, p , h . Escape cancels. A leader is not decoration. Every keystroke after it is a plain keypress that no browser and no window manager competes for, so one reservation buys the whole tree where a flat map would need one per command. Presets, if you already know another tool Settings, Input, Keyboard preset. Five ship: swe-mux, tmux, VS Code, Vim-flavoured, and Emacs. Each adds its own prefix and its own letters, and each also keeps the swe-mux leader tree, so choosing one never removes the route to a command that tool has no opinion about. You can pick one during first-time setup too. swe-mux is an outer shell, so any chord it claims is claimed from whatever runs inside a pane. The tmux preset takes Ctrl + B from a tmux running in a pane; Vim's takes Ctrl + W . Each preset says so before you choose it, and each can be rebound afterwards. Rebinding Settings, Input, Keyboard shortcuts. A binding is one to three chords; only the first needs Ctrl , Alt or Meta (or a function key), because everything after it is read while the shortcut is already armed. Shift alone is refused, because it shadows typing. Chords name the physical key rather than the character your layout produces, so a shortcut means the same key on QWERTY, AZERTY and Dvorak. Overrides are stored in keybindings.json in the data directory, and a binding for a command that was later renamed is migrated forward rather than dropped. Why not Ctrl+Alt Because on most non-US keyboards AltGr produces exactly Ctrl + Alt . swe-mux used to default to that namespace, which meant a German, French, Polish, Spanish or Nordic user fired application shortcuts while typing ordinary characters. No shipped preset uses it, and choosing one yourself is allowed but says so. What the terminal, the browser and your desktop keep swe-mux deliberately does not intercept the chords a shell or a TUI needs. Ctrl + C , Ctrl + D , Ctrl + R , Ctrl + L and their neighbours reach the process, which is why the application's own chords are on Ctrl + Shift rather than bare Ctrl . Two other layers get there first, and Settings names both. A browser tab keeps Ctrl + T , Ctrl + W and a handful more for itself, while the desktop app hands them to swe-mux - so a shortcut can be live in one and absent in the other, and the editor says which. Your window manager takes more still: on GNOME and KDE, Ctrl + Alt +arrows switch workspace and Ctrl + Alt + T opens a terminal. The list of chords a browser keeps is a guess about your browser, and browsers disagree. Settings, Input has a short test that measures it on the browser you are actually using, and the measurement overrides the guess. A chord you never test stays untested rather than being recorded as blocked. Mobile gestures The same command registry is bindable to touch gestures in Settings, Input, Touch gestures. Three defaults are worth knowing: Gesture Command Two-finger swipe up Open the current Project's notes Two-finger swipe down Read and select mode, which also lowers a keyboard being held up outside a terminal Swipe up on the command rail Toggle the swe-mux menu Horizontal swipe on the top bar Step through Projects, in sidebar order Voice reaches the same registry Spoken commands resolve through voice aliases on the ordinary command registry rather than through a parallel list, which is what keeps a command reachable by chord, palette, gesture, and voice without four definitions of it. Voice covers what is deliberately excluded from the spoken set."},{"u":"data/","t":"Where data lives, and what leaves the machine","s":"The data directory and what each part of it holds, exactly which network requests swe-mux makes, what is worth backing up, and what an uninstall leaves behind.","h":[["The data directory","the-data-directory"],["What is in it","what-is-in-it"],["The one request swe-mux makes on its own behalf","the-one-request-swe-mux-makes-on-its-own-behalf"],["Everything else that can reach the network","everything-else-that-can-reach-the-network"],["Backing it up","backing-it-up"],["What an uninstall leaves","what-an-uninstall-leaves"]],"b":"Where data lives, and what leaves the machine swe-mux runs on your machine and talks to no service this project operates. This page is the precise version of that sentence: where every file goes, and every request that can leave. The data directory Resolved in this order, and the order is the contract: MUX_DATA_DIR , if it is set and not empty. ~/.mux , if it already exists - on every host. An existing directory always wins, so a machine that has one keeps its data rather than silently starting from an empty one beside it. Otherwise the platform convention: ~/.mux on Windows, ~/Library/Application Support/swe-mux on macOS, and $XDG_DATA_HOME/swe-mux (else ~/.local/share/swe-mux ) on Linux. What is in it Kind What it holds Configuration config.toml (schema-versioned, with a pre-migration .bak ), settings.json for per-device browser settings, keybindings.json , and the automation rule files. Your content notes/ for the global Scratchpad, prompts/ for the prompt library, and media/ for session attachments. Databases mux.db holds history, telemetry, the status timeline, the deterministic facts, the prompt queue, schedules, clipboard history, the code graph, and the durable session registry. land-queue.sqlite3 holds the land queue. Credentials Saved provider logins, the metered-API key store, and the web-push identity. On Windows the key values are encrypted for the current user; on macOS the Keychain and on Linux the secret service hold them instead. Logs daemon.log , access.log , the supervisor's own logs, and the lifecycle ledger. All of them rotate, so a noisy day cannot grow an unbounded file. Regenerated per run The agent launcher shims, per-harness hook and MCP registration files, per-session identity, process-discovery files, and certificate material. All safely deletable. Media and capture Synthesized speech clips (bounded, 200 MB by default), downloaded speech models, preview screenshots, and terminal recovery checkpoints (bounded per session, per age, and per count). Your Projects' content is not in here and is not swe-mux's to hold. A Project points at a directory you already own. .swe-mux/ inside a checkout is per-machine state, not something the repository carries. The one request swe-mux makes on its own behalf A daily GET of https://swemux.dev/version.json , to find out whether a newer release exists. Nothing downloads. The file is byte-identical for every install, and the request carries no query string, no custom header, no cookie, and no identifier of the machine or the install. One switch turns it off, in Settings, Diagnostics, Software updates, and off means no request is made at all rather than a request that is discarded. Installing an update is a separate act you take: mux update --install <version> downloads that release, checks its SHA-256 against the published manifest before anything is staged, and refuses rather than installs if the release would need a new terminal supervisor - which would end your live sessions. Everything else that can reach the network Each of these is a feature you turn on, or your own agent CLI talking to its own vendor. None of them is on by default. Your agent CLIs: They contact their vendors under your subscription. swe-mux is not in that path: it proxies nothing and resells nothing. Model-backed features: Summarization, the assistant, and some control-plane features call an OpenRouter-compatible endpoint with your own key , and are off until you configure one. Web push: Goes through your browser vendor's push service, which is how web push works, and only after you subscribe a device. On-device speech models: Downloaded once from the model host, pinned by revision and verified by SHA-256. Saved provider accounts: Poll that vendor's own usage endpoint, with the credential you saved. Experimental Edge TTS: Reaches a Microsoft endpoint, and requires an explicit versioned acknowledgement before any text leaves the machine. There is no analytics, no crash reporting, and no account. There is no swe-mux server for an account to exist on. Backing it up Stop the daemon first, or accept that the database's write-ahead log files are part of the copy - a data directory copied while a daemon is running leaves a log the restoring host has to recover. mux.db and land-queue.sqlite3 , each with their -wal and -shm siblings if present. config.toml , settings.json , keybindings.json , and the automation rule files. prompts/ , notes/ , media/ . The provider accounts directory, the metered-key store, and the push identity. These are credentials - treat the backup as a secret. Every log and everything regenerated per run is safely disposable. So is the synthesized speech cache, the downloaded models, and the recovery checkpoints, all of which cost only re-download or re-capture. What an uninstall leaves Uninstalling the package removes the three commands and the code. It does not touch the data directory, and it does not touch your agent CLIs' transcripts - swe-mux only ever read those where the vendor wrote them. Removing the data directory is a separate, deliberate act."},{"u":"troubleshooting/","t":"Troubleshooting","s":"The things that actually go wrong: a blank page, nothing on PATH, a daemon that will not start, sessions that look lost, a phone that cannot use the microphone.","h":[["Nothing is on my PATH after installing","nothing-is-on-my-path-after-installing"],["There is no desktop shortcut or Start Menu entry","there-is-no-desktop-shortcut-or-start-menu-entry"],["The page loads but there is no interface","the-page-loads-but-there-is-no-interface"],["The daemon will not start","the-daemon-will-not-start"],["My sessions look lost after a restart","my-sessions-look-lost-after-a-restart"],["swe-mux cannot see an agent CLI I have installed","swe-mux-cannot-see-an-agent-cli-i-have-installed"],["The microphone does not work on my phone","the-microphone-does-not-work-on-my-phone"],["A queued message never got sent","a-queued-message-never-got-sent"],["A session says awaiting and looks stuck","a-session-says-awaiting-and-looks-stuck"],["Getting a diagnostic somebody can read","getting-a-diagnostic-somebody-can-read"],["Something is still wrong","something-is-still-wrong"]],"b":"Troubleshooting Start with mux doctor . It is read-only, it works whether or not a daemon is running, and every failing row carries a remedy line, so the next step is not a documentation hunt. Nothing is on my PATH after installing This is the ordinary outcome of pip install , whose WARNING: The scripts ... are installed in '...' which is not on PATH scrolls past unread. pip installs into whichever environment is currently active and puts nothing on PATH globally. # The daemon, needing no PATH setup at all: `python -m swe_mux` is exactly `muxd`. python -m swe_mux # Where the three executables actually went. python -c \"import sysconfig; print(sysconfig.get_path('scripts'))\" # Or ask swe-mux itself. swemuxd --where uv tool install swe-mux and pipx install swe-mux both put all three on PATH globally, and are the recommended forms for that reason. There is no desktop shortcut or Start Menu entry There never will be from a Python install. Wheels have no post-install hook and pip runs no install-time code, so this is structural rather than a step somebody forgot. On Windows, mux install-shortcut creates them afterwards, and --remove takes them away again. The page loads but there is no interface Not a bug. The frontend bundle is build output and is not carried in the repository, so a fresh clone, a fresh worktree, and a CI checkout all have none: the daemon answers the API perfectly and serves no interface. npm --prefix frontend ci npm --prefix frontend run build mux doctor distinguishes the two cases that look identical from the browser. In a source checkout the missing bundle is a warning carrying exactly that command. In an installed copy it is a failure , because a wheel that shipped without an interface is a broken artifact, and reinstalling from a complete one is the fix. The daemon will not start Run mux doctor . With nothing listening it produces the local report, and the first FAIL is the one to fix. Four causes account for most of them: A config that does not validate: The config check fails with the real parse or validation error. Fix config.toml , or move it aside - a removed config is rewritten with defaults on the next start. This is the fault the CLI otherwise hides, because a config failure makes every mux command fall back to the loopback default and possibly point at the wrong daemon. The port is already held: The port check fails and names the owner-finding command for your host ( netstat -ano | findstr :<port> on Windows, ss -ltnp on Linux). Stop the owner, or set a different port. A broken install: The import check fails with the real exception attached. On Windows the specific risk is the one compiled dependency in the runtime closure, where a wheel mismatch or a missing Visual C++ runtime surfaces as an import error. An unwritable or missing data directory: The check distinguishes \"exists and cannot be written\" from \"does not exist and cannot be created\", and points at MUX_DATA_DIR . A bound listener is not a ready daemon. Health answers 503 with the startup phase still in flight until the runtime exists, so \"it is listening but everything 503s\" is the daemon still starting rather than a fault. muxd --local-only takes tailnet detection out of the startup path when you are isolating a network problem. My sessions look lost after a restart Which mechanism applies decides what you can get back, and there are two: The PTY supervisor, and it ships off: With it on, terminals are held by a separate process and survive a daemon restart, an app rebuild, and a redeploy. With it off, a restart reaps every session - which is why the restart endpoint refuses outright unless it is forced. Turn it on in Settings, Terminals. Cold recovery, which ships on: Independent of the supervisor, and it covers what the supervisor cannot: sessions whose daemon and terminal owner both died. They come back as visible, dead, resumable rows rather than vanishing. Check the state before concluding anything. mux doctor against a running daemon reports supervisor attachment, and mux doctor --export lists cold sessions with their reason and capture state. swe-mux cannot see an agent CLI I have installed swemux harnesses # every harness in the registry, and what it resolved to If it resolved to the wrong binary, or to none, set the path for that harness in Settings, Harnesses. That is the usual fix on a machine carrying several installs of the same CLI. A harness the registry has never heard of is a different situation and not a fault: it runs perfectly in a real terminal, it just gets no layer on top. The microphone does not work on my phone Browsers restrict the microphone and the clipboard outside a secure context, so this is almost always HTTPS rather than swe-mux. In order: Confirm a daemon is listening on the port your configuration actually names. Confirm tailscale serve status proxies port 443 to that port. Confirm the phone is reaching the .ts.net hostname . The certificate is bound to the hostname, so the raw 100.x address cannot serve HTTPS at all. Confirm Use Tailscale DNS is on, and that Android's Private DNS is off or automatic. A queued message never got sent Automatic delivery is off by default. A staged message waits for you to send it. If you queued three and expected them to flow, that is the design rather than a fault. The prompt queue covers turning it on and what the readiness gate and stability window mean. A session says awaiting and looks stuck Awaiting means the agent is waiting on you - an approval, a question, a choice. It is not a stall. Read the pane. Idle is the one that is genuinely ambiguous, and swe-mux never reports it as finished on its own: an agent waiting on you and an agent with background work still running look identical in a quiet terminal and mean the opposite. Getting a diagnostic somebody can read swemux doctor --export > diagnostics.json That is the full bundle as JSON: the sanitized configuration, remote-connection state, firewall status, network counters, fleet status health, the status timeline and recovery sink statistics, any cold sessions, and the tails of the daemon log. It is an artifact to attach to a bug report rather than a table to read. It is sanitized rather than raw, and the full report reads no secret, no terminal byte, and no message content. Read it before you attach it anyway. Something is still wrong Bugs go to the issue tracker , with the export attached. Feature requests go to the Ideas discussions , where a thumbs-up is a vote."},{"u":"contributing/","t":"Developing swe-mux","s":"Maintainer material: running from a checkout, the verification gate a change has to pass, the DCO sign-off, and the extra rules a dependency change carries.","h":[["Running from a checkout","running-from-a-checkout"],["The verification gate","the-verification-gate"],["Contributions arrive under a DCO","contributions-arrive-under-a-dco"],["A dependency change carries extra rules","a-dependency-change-carries-extra-rules"],["Where the rest is","where-the-rest-is"],["Reporting rather than contributing","reporting-rather-than-contributing"]],"b":"Developing swe-mux This page is maintainer material. Everything else under /docs/ is written for somebody using swe-mux; this one is for somebody changing it, and it is the only page that sends you into the repository. Running from a checkout git clone https://github.com/jatoran/swe-mux cd swe-mux uv sync npm --prefix frontend ci npm --prefix frontend run build # a fresh clone serves no UI until this runs once uv run swe-mux The frontend bundle is git-ignored build output. A fresh clone serves a blank page rather than an error until that build has run once. The verification gate Run it before opening a pull request. It is the same set continuous integration runs. uv run pytest tests -q -n auto --dist loadgroup uv run ruff check src/swe_mux tests packaging uv run mypy npx tsc --noEmit # in frontend/ npm test # in frontend/ The Python suite runs across the host's cores: about 45 seconds on a 16-core machine against 242 serial, plus about 17 for the four checks after it. Continuous integration mirrors the same set on Windows and adds Linux and macOS legs, the production frontend build, and the full browser renderer suite. Contributions arrive under a DCO git commit -s , not a contributor licence agreement. A CLA would let the project relicense your contribution later. A developer certificate of origin does not, and that is the intended trade. A dependency change carries extra rules The rule the licence gate exists to enforce is that a dependency's declared licence does not describe what its wheel ships . So the check has two halves that must both stay alive: one reads the resolved dependency closure's metadata and runs in the verification gate, and the other reads the built desktop bundle for payloads by artifact name. Neither substitutes for the other. GPL and AGPL may never enter. LGPL needs an allowlist entry and must ship as replaceable source inside the bundle. The third-party notices file and its data are both generated. Never hand-edit either; run the audit and commit what it writes. Where the rest is The repository's own root documents are the ones to read, and they are written for this audience: CONTRIBUTING.md: What a change has to satisfy : the sign-off, the gate, and the dependency rules in full. CLAUDE.md: The working rules for this repository , including the reload flows that let you apply a change without ending live sessions. SECURITY.md: Where a vulnerability report goes , and the trust boundary it is measured against. The design documents: The maintained design contract lives in the repository under .docs/ . It is written for whoever maintains a subsystem next: it states invariants and the decisions already made, names incident dates, and is not written to be read as documentation. That is exactly why the pages you are reading exist beside it rather than linking into it. The site itself: site/README.md is the design record for swemux.dev, including how these pages are generated and the gates that keep them honest. Reporting rather than contributing Issues are for bugs. Ideas discussions are for feature requests, where a thumbs-up is a vote and the most-voted open ideas are drawn on the roadmap . Describe the problem rather than the fix."}];
